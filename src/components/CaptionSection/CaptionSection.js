import React from 'react'

function CaptionSeaction(props) {
    return (
        <div className="caption-section">
            {props.children}
        </div>
    )
}

export default CaptionSeaction;